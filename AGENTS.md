# Project Instructions

## 项目目标

打造一个使用最新且稳定技术栈的 uni-app 模板。

## 维护原则

- 优先使用 uni-app 和相关工具链的最新正式版，不主动使用 Alpha 或其他预发布版本。
- 更新依赖后执行类型检查，并验证目标平台可以正常构建或启动。
- 保持依赖版本统一，提交 `package-lock.json`，确保安装结果可复现。
- 不使用 `npm audit fix --force` 强行升级依赖，避免破坏 uni-app 工具链兼容性。
- 修改项目配置或依赖时，同步更新 `README.md`。

## 开发约定

- 使用 Wot Design Uni 作为 UI 组件库，优先使用组件完成布局，保持样式简洁。
- 使用 Pinia 管理主题和用户登录态，主题和语言切换结果需要持久化。
- 使用 `AppPage` 统一页面容器；页面导航使用 `AppNavbar`，底部导航使用 `AppTabbar`。
- 设置页使用 `AppPage` 的 `title` 显示导航栏，TabBar 页面传入 `showTabbar`。
- API 按业务模块放在 `src/api`，页面通过 API 模块调用，不直接拼接请求。
- `src/utils/request.ts` 统一处理请求、Token 和错误提示，成功条件按后端返回的 `result.code === 200` 判断，并直接返回 `result.data`。
- 公共请求层不判断 `data` 是否为空；部分成功接口可以返回 `null`，由具体 API 的调用方校验业务必需字段。
- 异步请求优先使用 `await-to-js` 的 `[error, data]` 写法，避免重复的 `try...catch`。
- 请求错误统一使用 `uni.showToast` 提示；页面不重复解析接口错误响应。
- 修改后运行项目中已有的检查命令；如果没有对应脚本，不要擅自新增检查工具。不主动执行耗时的打包命令。

## 通用能力规划

模板应优先提供跨平台差异较大的通用能力基础封装，页面通过 `src/utils` 或业务 API 调用，不直接散落平台判断。涉及平台能力时，明确区分 H5、App 和各小程序的实现差异，并提供失败、取消、权限拒绝和不支持平台的处理。

建议逐步补充以下能力：

- 小程序版本更新：封装 `uni.getUpdateManager`，处理发现新版本、下载完成、更新失败，并允许在启动时检查。
- App 版本升级：封装应用版本检查、强制升级和可选升级；下载 APK/安装包时展示进度，区分应用内更新与跳转应用市场。
- 图片上传：统一选择图片、压缩、尺寸/数量限制、上传进度、取消上传和服务端 URL 返回；上传接口放在 `src/api/file.ts`。
- 文件上传与下载：统一文件选择、下载进度、暂停/取消、保存到本地和打开文件，并处理平台权限。
- 权限管理：封装相机、相册、麦克风、定位、存储等权限检查、申请和拒绝后的引导。
- 分享能力：统一 App、小程序和 H5 的分享配置，覆盖页面分享、好友分享和复制链接兜底。
- 支付能力：按平台封装微信、支付宝、App 支付，统一支付中、成功、取消和失败状态。
- 定位与地图：统一定位授权、坐标转换、逆地理编码和地图跳转，并处理定位失败。
- 推送与通知：封装推送注册、设备标识、通知点击和冷启动参数，避免页面自行处理平台回调。
- 网络状态：监听网络变化，提供离线提示、请求重试和恢复网络后的刷新策略。
- 本地缓存：统一缓存键名、过期时间、JSON 序列化和清理策略，避免页面直接散落 `uni.setStorage`。
- 长列表与分页：提供分页参数、下拉刷新、触底加载、空状态、错误重试和防重复请求约定。
- 统一反馈：保留 `uni.showToast` 作为请求层兜底；页面内确认和复杂反馈使用 Wot Design Uni 组件。
- 监控与日志：预留错误上报、接口耗时、关键操作和版本信息采集开关，生产环境默认关闭敏感数据日志。

推荐目录示例：

```text
src/api/file.ts          # 上传、下载接口
src/api/app.ts           # 版本检查和升级接口
src/utils/update.ts      # 小程序/App 更新能力
src/utils/upload.ts      # 图片和文件上传封装
src/utils/download.ts    # 文件下载、保存和打开
src/utils/permission.ts  # 跨平台权限处理
src/utils/share.ts       # 分享能力
src/utils/platform.ts    # 平台判断与能力检测
```

每项能力至少应定义：调用入口、参数和返回值、支持的平台、加载/进度状态、取消方式、错误码处理，以及是否需要后端配合。
